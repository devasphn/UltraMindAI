# requirements.txt (Corrected for XTTS-v2)
# Core ML
torch==2.3.0
transformers==4.41.1
accelerate==0.30.1
bitsandbytes==0.43.1

# ASR & Audio
librosa

# TTS (The New Model)
TTS==0.22.0

# Web Server & WebRTC
aiohttp
aiortc
uvloop
numpy
av
